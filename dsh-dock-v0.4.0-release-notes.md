# dsh-dock v0.4.0 — 托盘常驻 + 开机自启

> 基于 **v0.3.7**（含其全部能力：优雅退出 appExit、stop.log、多候选自绘卡片、
> 「刷新」改名、10 秒超时、batch 文件名校验）。新能力：鲸鱼常驻系统托盘，
> 服务器状态一眼可见（tooltip），「重启服务器」一键完成，开机可选后台预热。

## 新增能力

| 能力 | 行为 | 入口 |
|---|---|---|
| 托盘常驻（默认开，可关） | 双击桌面鲸鱼开窗后，鲸鱼落入托盘并驻留；关窗不停服，托盘左键秒开 | 自动（可在设置中关闭退回短命行为） |
| 托盘状态 tooltip | 悬停显示「运行中 / 启动中… / 未运行 / 启动失败 · 端口与日志路径」，图标恒为黑鲸鱼 | 悬停托盘鲸鱼 |
| 重启服务器 | 写 .stopping → 杀监听进程 → 等端口死透 → 重新拉起 → 就绪后自动开窗（此前需完全退出再双击，~15s 手动流程收成一次点击） | 托盘右键「重启服务器」 |
| 开机自启（默认关） | 勾选写 `HKCU\...\Run`（`dsh-dock` = suite exe `--boot`）：登录后静默驻留托盘 + 后台拉起服务器，**不开窗**；点托盘秒开（服务器已预热） | 托盘右键「☑开机自启」或设置面板 |
| dsh-dock 设置页 | 所有插件设置归一处，注册为系统设置弹窗里的正式分区（`settings.section`，与其他插件设置页同构）：「☑ 托盘常驻」「☑ 开机自启」；经 `/launcher/api/settings/{get,set}` 读写（回环信任围栏与 stop 路由一致） | 设置弹窗 →「DSH Dock（启动器）」 |
| 完全退出（托盘侧） | **单击直执行** → 停服务器 → 关闭所有 dsh-dock 窗口（按 profile 身份） → 清理 .stopping → 托盘消失 | 托盘右键「完全退出」 |

## 行为变更（相对 v0.3.x）

- **双击桌面鲸鱼后进程不再退出**（默认）：开窗后转入托盘常驻。旧「开窗即退」
  行为可在设置页/托盘菜单关闭「托盘常驻」恢复。
- **README「无常驻进程」声明不再成立**：托盘模式引入一个常驻的启动器进程
  （仍是单 exe、只与本机回环通信、不读不传凭据）。这是用户主动换取的状态
  入口，且默认可关。
- 托盘已在驻留时再次双击桌面鲸鱼：新进程走快路径开窗后即退出，托盘不动（零 IPC）。
- **托盘单实例硬闸（命名互斥锁）**：任何时刻托盘至多一个图标。旧检查按进程名比对，
  而 suite 副本（`dsh-dock-launcher.exe`）与桌面副本（`DSH Harness.exe`）名字不同，
  会误判「无托盘」导致第二个图标；现改为 `Local\dsh-dock-tray-<port>` 命名互斥锁——
  名字无关、天然免疫并发竞态、持有者崩溃时自动释放。
- **托盘勾选状态弹出即刷新**：开机自启勾选改为每次右键弹出时重读注册表，
  修复「设置页改了自启、托盘菜单仍显示旧状态」的陈旧勾选。
- **完全退出按身份关窗（不靠记账）**：改为枚举 msedge 进程命令行，凡带专属
  `--user-data-dir=edge-app-profile` 的一律优雅关闭+超时强杀，修复「托盘实例
  更换后旧托盘开的窗口成孤儿不被关闭」。托盘「完全退出」为**单击直执行**
  （右键菜单本身即是明确意图；侧栏菜单保留页内两步防误触）。
- **重启服务器双入口 + 先关旧窗再开新窗**：托盘右键与侧栏「更多」菜单都可触发；
  侧栏走 `/launcher/api/restart` 写入 `.restart.request`，驻留托盘检测后执行完整
  重启序列（宿主无法重启自己的死亡，托盘是执行者，实测托盘在重启中存活）。
  重启先按 profile 身份关闭所有旧 dock 窗口（它们的 token 已随旧进程作废），
  服务器就绪后只自动打开一个全新窗口（用户实测确认）。
- **侧栏「完全退出」同步清托盘**：在返回响应前同步杀托盘（进程名校验防 PID 复用），
  排在宿主自身退出之后的清理步骤实测会随宿主死亡丢失。
- **界面语言跟随 DSH**：插件自身文案（侧栏菜单、设置页、退出遮罩）改为经宿主
  `locale` 服务注册 `dsh-dock` 中/英字典，切换语言即时重渲染；侧栏菜单项定名「刷新」
  定名为「刷新」（仅重载页面，服务器不动）。原生托盘 exe 暂未本地化（Windows 原生 UI）。

## 实现落点

- `src/DshDockLauncher.cs`：`DshTray` 类（NotifyIcon 生命周期、后台 poller 探测
  状态、`--boot` 冷启、重启序列、托盘侧完全退出、trayPid 落盘 launcher.json、
  HKCU Run 读写）；`LauncherConfig` 增 `SETTINGS`/`MANIFEST` 键（旧 ini 自动按
  目录推导）。
- `index.js`：`/launcher/api/settings/{get,set}` 路由（settings.json + 注册表）、
  stop 路由追加杀 trayPid、persistManifest 改为合并写（保留 trayPid）、ini 增
  `SETTINGS`/`MANIFEST` 键。
- `client.js`：`DockSettingsPage` 注册进 `settings.section`（设置弹窗内的正式分区，
  托盘常驻/开机自启勾选），侧栏菜单「设置」转发到系统设置弹窗。

## 升级须知

- 桌面 exe 与 `~\.dsh\launcher\dsh-dock-launcher.exe` 会在插件激活时自动刷新
  （内容比对；运行中的镜像走 rename-aside 替换）。
- 升级后首次「完全退出」再双击，即可观察到托盘鲸鱼驻留。
- 开机自启默认**关**：需要的人托盘右键勾选或在设置面板勾选。
